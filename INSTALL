These are the instructions for GNUstep system.  For Apple Mac OS X
systems, see the INSTALL.OSX file.

You need GNUstep installed (with both the development headers and the
graphical libraries) to compile this package.  Make sure you have a
recent version of gnustep-make (>= 2.0.5).

To compile, type 'make'
To install, type 'make install'

To learn more about how to use Renaissance, you can look at the
documentation in Documentation, and at the example applications in
Examples/Applications/.
